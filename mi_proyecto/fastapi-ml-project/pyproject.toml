[project]
name = "fastapi-ml-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Joshin290", email = "alexandervl1689@gmail.com" }
]
requires-python = ">=3.10.9"
dependencies = [
    "fastapi>=0.141.1",
    "numpy>=2.2.6",
    "pandas>=2.3.3",
    "scikit-learn>=1.7.2",
    "uvicorn>=0.52.4",
]

[project.scripts]
fastapi-ml-project = "fastapi_ml_project:main"

[build-system]
requires = ["uv_build>=0.12.9,<0.13.0"]
build-backend = "uv_build"
